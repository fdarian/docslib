{
	"name": "@docslib/monorepo",
	"workspaces": [
		"packages/*",
		"apps/*",
		"internals"
	],
	"devDependencies": {
		"@biomejs/biome": "^2.3.13",
		"@changesets/cli": "^2.29.4",
		"turbo": "^2.8.1"
	},
	"scripts": {
		"docslib-in": "bun internals/entries/cli.ts",
		"link": "cd apps/cli && bun link && cd ../mcp && bun link",
		"unlink": "cd apps/cli && bun unlink && cd ../mcp && bun unlink",
		"build": "turbo build",
		"check": "turbo check:tsc",
		"changeset": "changeset",
		"ci:version": "changeset version && bun scripts/sync-versions.ts && bun install --no-frozen-lockfile",
		"ci:release": "bun run build && bun scripts/publish-npm.ts"
	},
	"packageManager": "bun@1.3.8"
}
